<!--pages/routeDraw/routeDraw.wxml-->
<map
        class="mapView"
        style="width: 100%; height: 100vh;"
        latitude="{{latitude}}"
        longitude="{{longitude}}"
        scale="100"
        polyline="{{polyline}}"
        markers="{{markers}}"
        show-location="{{false}}"
></map>
